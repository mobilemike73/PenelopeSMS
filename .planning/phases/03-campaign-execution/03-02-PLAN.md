---
phase: 03-campaign-execution
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/PenelopeSMS.Infrastructure/SqlServer/Queries/CampaignRecipientSelectionQuery.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Repositories/CampaignRepository.cs
  - src/PenelopeSMS.App/Workflows/ICampaignCreationWorkflow.cs
  - src/PenelopeSMS.App/Workflows/CampaignCreationWorkflow.cs
  - src/PenelopeSMS.App/Menu/CampaignMenuAction.cs
  - src/PenelopeSMS.App/Menu/MainMenu.cs
  - src/PenelopeSMS.App/Program.cs
  - tests/PenelopeSMS.Tests/Campaigns/CampaignCreationWorkflowTests.cs
autonomous: true
requirements:
  - CAMP-01
  - CAMP-02
  - CAMP-03
  - CAMP-06
must_haves:
  truths:
    - The operator can create a campaign from a plain-text template file and a chosen batch size without sending messages yet.
    - Campaign recipient materialization includes only eligible canonical phone records and excludes duplicates by construction.
    - Campaign creation produces console-visible counts the operator can review before moving on to send execution.
  artifacts:
    - src/PenelopeSMS.App/Workflows/CampaignCreationWorkflow.cs
    - src/PenelopeSMS.Infrastructure/SqlServer/Queries/CampaignRecipientSelectionQuery.cs
    - src/PenelopeSMS.App/Menu/CampaignMenuAction.cs
  key_links:
    - The campaign-creation query builds recipients from `PhoneNumberRecord.CampaignEligibilityStatus == Eligible`.
    - The workflow snapshots template file path and body text into the campaign row and stores the configured batch size.
    - Duplicate prevention comes from campaign-recipient materialization onto canonical phone rows, not from source-row filtering alone.
---

<objective>
Implement campaign drafting so the operator can create a campaign from a text file and materialize its eligible recipients locally.

Purpose: Campaign execution should be a two-step workflow. This plan creates the durable draft and recipient set the send workflow will consume later.
Output: Recipient selection query, campaign repository and creation workflow, console draft action, and automated drafting tests.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-campaign-execution/03-RESEARCH.md
@.planning/phases/03-campaign-execution/03-VALIDATION.md
@.planning/phases/03-campaign-execution/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recipient-selection and campaign-persistence support</name>
  <files>src/PenelopeSMS.Infrastructure/SqlServer/Queries/CampaignRecipientSelectionQuery.cs, src/PenelopeSMS.Infrastructure/SqlServer/Repositories/CampaignRepository.cs</files>
  <action>Create query and repository support to build campaign recipients from eligible phone numbers only, persist the campaign draft, and insert one outbound recipient row per eligible canonical phone. Persist the chosen batch size and template snapshot on the campaign row.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The app can deterministically materialize a campaign draft and its deduplicated recipient set.</done>
</task>

<task type="auto">
  <name>Task 2: Build the campaign-creation workflow and console menu path</name>
  <files>src/PenelopeSMS.App/Workflows/ICampaignCreationWorkflow.cs, src/PenelopeSMS.App/Workflows/CampaignCreationWorkflow.cs, src/PenelopeSMS.App/Menu/CampaignMenuAction.cs, src/PenelopeSMS.App/Menu/MainMenu.cs, src/PenelopeSMS.App/Program.cs, src/PenelopeSMS.Infrastructure/SqlServer/Repositories/CampaignRepository.cs</files>
  <action>Implement a workflow that prompts for a template file path and batch size, loads and validates the template, creates the campaign draft, persists the deduplicated eligible recipient ledger, and prints console-visible summary counts for drafted recipients and skipped ineligible numbers.</action>
  <verify><automated>dotnet build PenelopeSMS.sln && dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The console can create a durable campaign draft that is ready for batched sending.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated tests for template-driven campaign drafting</name>
  <files>tests/PenelopeSMS.Tests/Campaigns/CampaignCreationWorkflowTests.cs</files>
  <action>Add tests that verify a campaign can be created from a plain-text file, only eligible canonical phone numbers become recipients, duplicate canonical numbers are not inserted twice into the same campaign, and the chosen batch size is stored on the campaign.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>The campaign drafting workflow has automated coverage for Phase 3’s recipient-creation and duplicate-prevention rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build PenelopeSMS.sln` passes
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] The console can create a campaign draft from a plain-text template file and display recipient counts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The operator can create campaign drafts with deduplicated eligible recipients and stored batch size
</success_criteria>

<output>
After completion, create `.planning/phases/03-campaign-execution/03-02-SUMMARY.md`
</output>
