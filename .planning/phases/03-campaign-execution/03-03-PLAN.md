---
phase: 03-campaign-execution
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/PenelopeSMS.Infrastructure/SqlServer/Queries/CampaignSendBatchQuery.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Repositories/CampaignSendRepository.cs
  - src/PenelopeSMS.App/Workflows/ICampaignSendWorkflow.cs
  - src/PenelopeSMS.App/Workflows/CampaignSendWorkflow.cs
  - src/PenelopeSMS.App/Menu/CampaignMenuAction.cs
  - src/PenelopeSMS.App/Menu/MainMenu.cs
  - tests/PenelopeSMS.Tests/Campaigns/CampaignSendWorkflowTests.cs
autonomous: true
requirements:
  - CAMP-03
  - CAMP-04
  - CAMP-05
must_haves:
  truths:
    - The operator can send a drafted campaign in configurable batch sizes from the console.
    - Every accepted outbound message persists a Twilio Message SID and the initial provider status.
    - The send workflow consumes only pending campaign-recipient rows so already-submitted recipients are not resent during the same campaign.
  artifacts:
    - src/PenelopeSMS.App/Workflows/CampaignSendWorkflow.cs
    - src/PenelopeSMS.Infrastructure/SqlServer/Queries/CampaignSendBatchQuery.cs
    - tests/PenelopeSMS.Tests/Campaigns/CampaignSendWorkflowTests.cs
  key_links:
    - The send workflow takes the campaign’s stored batch size and applies it to pending recipient rows in deterministic order.
    - Twilio send results update the campaign-recipient ledger with provider SID, initial status, and any immediate provider error details.
    - Console-visible send summaries report how many recipients were attempted, accepted, failed immediately, and left pending.
---

<objective>
Implement batched campaign sending through Twilio and persist the provider response to the outbound ledger.

Purpose: This delivers the core user-facing value of Phase 3: sending drafted campaigns in controllable batches while storing the provider correlation data needed for later delivery tracking.
Output: Batch-send query and repository support, send workflow, console action, and automated send tests.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-campaign-execution/03-RESEARCH.md
@.planning/phases/03-campaign-execution/03-VALIDATION.md
@.planning/phases/03-campaign-execution/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pending-recipient batch selection and send-state persistence</name>
  <files>src/PenelopeSMS.Infrastructure/SqlServer/Queries/CampaignSendBatchQuery.cs, src/PenelopeSMS.Infrastructure/SqlServer/Repositories/CampaignSendRepository.cs</files>
  <action>Create the query and repository support needed to fetch pending recipient rows for a campaign in deterministic order, apply the campaign’s stored batch size, and persist the synchronous Twilio response fields onto each recipient row after a send attempt.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The app has a safe, deterministic batch-send data path that updates the local outbound ledger per recipient.</done>
</task>

<task type="auto">
  <name>Task 2: Build the campaign-send workflow and console action</name>
  <files>src/PenelopeSMS.App/Workflows/ICampaignSendWorkflow.cs, src/PenelopeSMS.App/Workflows/CampaignSendWorkflow.cs, src/PenelopeSMS.App/Menu/CampaignMenuAction.cs, src/PenelopeSMS.App/Menu/MainMenu.cs, src/PenelopeSMS.Infrastructure/SqlServer/Repositories/CampaignSendRepository.cs</files>
  <action>Implement a workflow that lets the operator choose a drafted campaign, send the next batch using the stored batch size, call the Twilio message sender for each pending recipient in the batch, persist the Message SID plus initial provider status, and print batch summary counts to the console.</action>
  <verify><automated>dotnet build PenelopeSMS.sln && dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The console can send drafted campaigns in controllable batches while recording the provider response needed for delivery tracking.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated tests for batch sending and provider-response persistence</name>
  <files>tests/PenelopeSMS.Tests/Campaigns/CampaignSendWorkflowTests.cs</files>
  <action>Add tests that verify batch sizing is honored, accepted sends persist Twilio Message SID and initial status, immediate send failures persist provider error details, and already-submitted recipients are not resent during the same campaign.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>The send workflow has automated coverage for batching behavior and persistence of immediate Twilio send results.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build PenelopeSMS.sln` passes
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] The console can send a drafted campaign batch and store provider Message SID plus initial status
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The operator can send campaign batches through Twilio and persist the synchronous provider response per recipient
</success_criteria>

<output>
After completion, create `.planning/phases/03-campaign-execution/03-03-SUMMARY.md`
</output>
