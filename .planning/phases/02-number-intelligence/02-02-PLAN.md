---
phase: 02-number-intelligence
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/PenelopeSMS.App/Workflows/IEnrichmentWorkflow.cs
  - src/PenelopeSMS.App/Workflows/EnrichmentWorkflow.cs
  - src/PenelopeSMS.App/Menu/EnrichmentMenuAction.cs
  - src/PenelopeSMS.App/Menu/MainMenu.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Queries/EnrichmentTargetingQuery.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Repositories/PhoneNumberEnrichmentRepository.cs
  - tests/PenelopeSMS.Tests/Enrichment/EnrichmentWorkflowTests.cs
autonomous: true
requirements:
  - ENRH-01
  - ENRH-02
  - ENRH-03
  - ENRH-04
must_haves:
  truths:
    - The operator can run enrichment in a default mode that targets due records only, plus an explicit full-refresh mode.
    - Successful enrichments update provider facts and derived eligibility on the canonical phone record.
    - Failed enrichments update retryable/non-retryable failure state without losing the previous successful snapshot fields unless a new success arrives.
  artifacts:
    - src/PenelopeSMS.App/Workflows/EnrichmentWorkflow.cs
    - src/PenelopeSMS.App/Menu/EnrichmentMenuAction.cs
    - src/PenelopeSMS.Infrastructure/SqlServer/Queries/EnrichmentTargetingQuery.cs
  key_links:
    - The targeting query decides which phone records are due based on never-enriched, failed, stale-success, and full-refresh rules.
    - The enrichment workflow reads Twilio facts through the adapter from Plan 02-01 and persists the latest snapshot back to SQL Server.
    - Workflow tests prove that default vs full refresh behavior matches the Phase 2 context decisions.
---

<objective>
Implement the operator-facing enrichment run that targets due phone records and persists the latest Twilio facts.

Purpose: This is the core functional outcome of Phase 2: the operator can launch enrichment and update stored provider facts and eligibility without manually picking each phone record.
Output: Due-record targeting query, enrichment repository / workflow, menu integration, and automated workflow tests.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-number-intelligence/02-CONTEXT.md
@.planning/phases/02-number-intelligence/02-RESEARCH.md
@.planning/phases/02-number-intelligence/02-VALIDATION.md
@.planning/phases/02-number-intelligence/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement enrichment targeting for default and full-refresh modes</name>
  <files>src/PenelopeSMS.Infrastructure/SqlServer/Queries/EnrichmentTargetingQuery.cs, src/PenelopeSMS.Infrastructure/SqlServer/Repositories/PhoneNumberEnrichmentRepository.cs</files>
  <action>Create the query and persistence support needed to select due records for enrichment. Default mode must include never-enriched records, failed records, and successful records older than 30 days; full refresh must allow the operator to re-run all imported records explicitly.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The app has a deterministic due-record selection path that enforces the context-approved refresh rules.</done>
</task>

<task type="auto">
  <name>Task 2: Build the enrichment workflow and connect it to the console menu</name>
  <files>src/PenelopeSMS.App/Workflows/IEnrichmentWorkflow.cs, src/PenelopeSMS.App/Workflows/EnrichmentWorkflow.cs, src/PenelopeSMS.App/Menu/EnrichmentMenuAction.cs, src/PenelopeSMS.App/Menu/MainMenu.cs, src/PenelopeSMS.Infrastructure/SqlServer/Repositories/PhoneNumberEnrichmentRepository.cs</files>
  <action>Implement a workflow that lets the operator run default enrichment or full refresh from the console, calls the Twilio lookup adapter for each selected phone record, persists the latest provider facts and derived eligibility, and prints summary counts for processed, updated, failed, and skipped records.</action>
  <verify><automated>dotnet build PenelopeSMS.sln && dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The console can launch enrichment using the approved refresh modes, and the persistence layer reflects the latest Twilio facts and eligibility state after each run.</done>
</task>

<task type="auto">
  <name>Task 3: Add workflow tests for default targeting, full refresh, and record updates</name>
  <files>tests/PenelopeSMS.Tests/Enrichment/EnrichmentWorkflowTests.cs</files>
  <action>Add xUnit tests that exercise the enrichment workflow with a fake Twilio lookup client and SQLite persistence. Cover never-enriched selection, failed-record retry selection, stale-success refresh after 30 days, full refresh mode, and the persistence of updated Twilio facts plus derived eligibility.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>The enrichment workflow has automated coverage for the targeting rules and persisted outcomes that matter to Phase 2.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build PenelopeSMS.sln` passes
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] Default enrichment mode and full refresh mode both produce console-visible summary counts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The operator can run enrichment for due phone records and persist the latest Twilio facts plus derived eligibility
</success_criteria>

<output>
After completion, create `.planning/phases/02-number-intelligence/02-02-SUMMARY.md`
</output>
