---
phase: 02-number-intelligence
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/PenelopeSMS.App/Workflows/IEnrichmentRetryWorkflow.cs
  - src/PenelopeSMS.App/Workflows/EnrichmentRetryWorkflow.cs
  - src/PenelopeSMS.App/Menu/EnrichmentFailureMenuAction.cs
  - src/PenelopeSMS.App/Menu/EnrichmentMenuAction.cs
  - src/PenelopeSMS.App/Menu/MainMenu.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Queries/FailedEnrichmentReviewQuery.cs
  - tests/PenelopeSMS.Tests/Enrichment/EnrichmentRetryWorkflowTests.cs
autonomous: true
requirements:
  - ENRH-01
  - ENRH-04
must_haves:
  truths:
    - Failed enrichments are visible to the operator with retry-relevant details.
    - The operator can retry all retryable failures or retry selected failures from the console.
    - Non-retryable failures remain visible and ineligible rather than being silently retried.
  artifacts:
    - src/PenelopeSMS.App/Workflows/EnrichmentRetryWorkflow.cs
    - src/PenelopeSMS.App/Menu/EnrichmentFailureMenuAction.cs
    - tests/PenelopeSMS.Tests/Enrichment/EnrichmentRetryWorkflowTests.cs
  key_links:
    - The failed-record review query exposes the data the console needs to list retryable vs permanent failures.
    - The retry workflow reuses the targeting and Twilio update pipeline from Plan 02-02, but scopes the record set to retry choices.
    - Tests prove retry-all and retry-selected actions do not include non-retryable failures.
---

<objective>
Add the failed-record review and retry actions that make enrichment failures operationally usable.

Purpose: Phase 2 is not complete until failed enrichments are visible and the operator can take the retry actions defined in the context without guessing which records are safe to rerun.
Output: Failed-record review query, retry workflow, console retry actions, and automated retry tests.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-number-intelligence/02-CONTEXT.md
@.planning/phases/02-number-intelligence/02-RESEARCH.md
@.planning/phases/02-number-intelligence/02-VALIDATION.md
@.planning/phases/02-number-intelligence/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the failed-record review projection and visibility path</name>
  <files>src/PenelopeSMS.Infrastructure/SqlServer/Queries/FailedEnrichmentReviewQuery.cs, src/PenelopeSMS.App/Menu/EnrichmentFailureMenuAction.cs</files>
  <action>Create the query and console projection needed to list failed enrichments with phone number, last error details, retryable/non-retryable classification, and current eligibility state. Keep the list focused on failure review and retry; do not expand into full monitoring dashboards in this phase.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The operator can review failed enrichments with enough detail to understand what can be retried and what is permanently ineligible.</done>
</task>

<task type="auto">
  <name>Task 2: Implement retry-all and retry-selected enrichment actions</name>
  <files>src/PenelopeSMS.App/Workflows/IEnrichmentRetryWorkflow.cs, src/PenelopeSMS.App/Workflows/EnrichmentRetryWorkflow.cs, src/PenelopeSMS.App/Menu/EnrichmentFailureMenuAction.cs, src/PenelopeSMS.App/Menu/EnrichmentMenuAction.cs, src/PenelopeSMS.App/Menu/MainMenu.cs</files>
  <action>Implement retry actions that let the operator retry all retryable failures or pick selected failed records explicitly. Ensure retry-all excludes non-retryable failures automatically, and selected retries still validate that each chosen record is retryable before making a Twilio call.</action>
  <verify><automated>dotnet build PenelopeSMS.sln && dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The console exposes both retry modes from the approved context, and retry actions stay within the safe retryable failure set.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated tests for failed-record review and retry behavior</name>
  <files>tests/PenelopeSMS.Tests/Enrichment/EnrichmentRetryWorkflowTests.cs</files>
  <action>Add xUnit tests that verify failed-record review data is projected correctly, retry-all includes only retryable failures, retry-selected respects explicit record choices, and non-retryable failures remain visible and ineligible after review.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>The failure review and retry workflows have automated coverage for the operator actions and safety rules defined for Phase 2.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build PenelopeSMS.sln` passes
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] The console exposes a failed-record list plus retry-all and retry-selected actions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Failed enrichments are visible and actionable through the approved retry modes
</success_criteria>

<output>
After completion, create `.planning/phases/02-number-intelligence/02-03-SUMMARY.md`
</output>
