---
phase: 02-number-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/PenelopeSMS.Domain/Entities/PhoneNumberRecord.cs
  - src/PenelopeSMS.Domain/Enums/CampaignEligibilityStatus.cs
  - src/PenelopeSMS.Domain/Enums/EnrichmentFailureStatus.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Configurations/PhoneNumberRecordConfiguration.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Migrations/*AddPhoneEnrichmentSnapshot.cs
  - src/PenelopeSMS.Infrastructure/Twilio/ITwilioLookupClient.cs
  - src/PenelopeSMS.Infrastructure/Twilio/TwilioLookupClient.cs
  - src/PenelopeSMS.Infrastructure/Twilio/TwilioLookupResult.cs
  - src/PenelopeSMS.Infrastructure/DependencyInjection.cs
  - src/PenelopeSMS.Infrastructure/PenelopeSMS.Infrastructure.csproj
  - tests/PenelopeSMS.Tests/Enrichment/TwilioLookupClientTests.cs
  - tests/PenelopeSMS.Tests/Enrichment/PhoneEligibilityTests.cs
autonomous: true
requirements:
  - ENRH-02
  - ENRH-03
  - ENRH-04
must_haves:
  truths:
    - Each phone record can persist the latest Twilio country, line type, carrier, raw payload, and enrichment timestamps.
    - Eligibility is stored as an app-derived field separate from the raw provider facts.
    - Failure metadata distinguishes retryable from non-retryable outcomes per phone record.
  artifacts:
    - src/PenelopeSMS.Domain/Entities/PhoneNumberRecord.cs
    - src/PenelopeSMS.Infrastructure/Twilio/TwilioLookupClient.cs
    - src/PenelopeSMS.Infrastructure/SqlServer/Migrations/*AddPhoneEnrichmentSnapshot.cs
  key_links:
    - The Twilio lookup client maps Lookup v2 validation and line type intelligence fields into the internal enrichment result.
    - The SQL Server schema stores the latest enrichment snapshot and failure state on the canonical phone record.
    - Tests prove eligibility derivation and failure classification without live Twilio calls.
---

<objective>
Add the persistence model and Twilio Lookup adapter that Phase 2 needs before any enrichment workflow runs.

Purpose: Number Intelligence only works if the app can store the latest provider facts, keep derived eligibility separate, and classify failures before the console workflow is added.
Output: Enrichment fields on phone records, SQL Server migration, Twilio lookup interface and implementation, and automated mapping / eligibility tests.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-number-intelligence/02-CONTEXT.md
@.planning/phases/02-number-intelligence/02-RESEARCH.md
@.planning/phases/02-number-intelligence/02-VALIDATION.md
@.planning/phases/01-data-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the canonical phone schema for enrichment snapshot and failure state</name>
  <files>src/PenelopeSMS.Domain/Entities/PhoneNumberRecord.cs, src/PenelopeSMS.Domain/Enums/CampaignEligibilityStatus.cs, src/PenelopeSMS.Domain/Enums/EnrichmentFailureStatus.cs, src/PenelopeSMS.Infrastructure/SqlServer/Configurations/PhoneNumberRecordConfiguration.cs, src/PenelopeSMS.Infrastructure/SqlServer/Migrations/*AddPhoneEnrichmentSnapshot.cs</files>
  <action>Add the latest enrichment snapshot fields, derived eligibility fields, and failure / retry-state fields to the canonical phone record model. Generate an EF Core migration for the SQL Server schema update. Keep the latest snapshot only; do not introduce a historical enrichment table in this phase.</action>
  <verify><automated>dotnet build PenelopeSMS.sln && dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The database model can persist the latest Twilio facts, derived eligibility, last attempted/enriched timestamps, and retry-aware failure details for each phone record.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Twilio Lookup adapter and internal result contract</name>
  <files>src/PenelopeSMS.Infrastructure/Twilio/ITwilioLookupClient.cs, src/PenelopeSMS.Infrastructure/Twilio/TwilioLookupClient.cs, src/PenelopeSMS.Infrastructure/Twilio/TwilioLookupResult.cs, src/PenelopeSMS.Infrastructure/DependencyInjection.cs, src/PenelopeSMS.Infrastructure/PenelopeSMS.Infrastructure.csproj</files>
  <action>Add the official Twilio .NET helper library and implement a Lookup v2 adapter that requests `line_type_intelligence`, maps the returned provider facts into an internal result contract, preserves the full raw payload JSON, and classifies retryable vs non-retryable failures. Keep Twilio-specific types out of the app and domain layers.</action>
  <verify><automated>dotnet build PenelopeSMS.sln && dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The app has a testable Twilio lookup abstraction that returns the fields Phase 2 needs and hides provider transport details behind infrastructure.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated tests for Twilio mapping, eligibility derivation, and failure classification</name>
  <files>tests/PenelopeSMS.Tests/Enrichment/TwilioLookupClientTests.cs, tests/PenelopeSMS.Tests/Enrichment/PhoneEligibilityTests.cs</files>
  <action>Add xUnit tests that verify the lookup adapter maps validation and line-type fields correctly, that the app derives `eligible` only for US mobile numbers, and that unknown classifications and permanent failures remain ineligible while transient provider/system failures are marked retryable.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>The enrichment foundation has automated coverage for provider mapping, eligibility derivation, and retryable failure rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build PenelopeSMS.sln` passes
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] SQL Server migration exists for the enrichment schema update
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app can persist the latest enrichment snapshot and map Twilio Lookup responses through a stable internal contract
</success_criteria>

<output>
After completion, create `.planning/phases/02-number-intelligence/02-01-SUMMARY.md`
</output>
