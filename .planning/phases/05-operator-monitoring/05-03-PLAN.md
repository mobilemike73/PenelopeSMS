---
phase: 05-operator-monitoring
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src/PenelopeSMS.App/Menu/MainMenu.cs
  - src/PenelopeSMS.App/Menu/MonitoringMenuAction.cs
  - src/PenelopeSMS.App/Rendering/MonitoringScreenRenderer.cs
  - src/PenelopeSMS.App/Workflows/MonitoringWorkflow.cs
  - src/PenelopeSMS.App/Program.cs
  - tests/PenelopeSMS.Tests/Monitoring/MonitoringMenuActionTests.cs
  - tests/PenelopeSMS.Tests/Monitoring/MonitoringRuntimeIntegrationTests.cs
  - tests/PenelopeSMS.Tests/Host/HostBootstrapTests.cs
autonomous: true
requirements:
  - MON-04
  - OPER-02
must_haves:
  truths:
    - The console app exposes one shared monitoring screen with auto-refresh, in-place repainting, warnings, and a persistent live-delivery panel.
    - Campaign monitoring supports summary-first browsing plus drill-in for a selected campaign, including explicit inclusion of completed campaigns.
    - The operator can understand current campaign state and current operational warnings without querying Twilio or SQL manually.
  artifacts:
    - src/PenelopeSMS.App/Menu/MonitoringMenuAction.cs
    - src/PenelopeSMS.App/Rendering/MonitoringScreenRenderer.cs
    - tests/PenelopeSMS.Tests/Monitoring/MonitoringRuntimeIntegrationTests.cs
  key_links:
    - The user wants one shared monitoring view, not separate subsystem dashboards.
    - Auto-refresh must repaint in place rather than append snapshots continuously.
    - Failure detail belongs in drill-in while the main screen stays summary-first and warnings-focused.
---

<objective>
Deliver the operator-facing monitoring UI in the console app with shared dashboard behavior, campaign drill-in, and runtime integration coverage.

Purpose: Phase 5 is only complete when the operator can open one monitoring surface, watch live job state and delivery activity, and inspect campaign progress without leaving the app.
Output: Monitoring menu entry, renderer, auto-refreshing dashboard, campaign drill-in behavior, and automated menu/runtime tests.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operator-monitoring/05-CONTEXT.md
@.planning/phases/05-operator-monitoring/05-RESEARCH.md
@.planning/phases/05-operator-monitoring/05-VALIDATION.md
@.planning/phases/05-operator-monitoring/05-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the shared monitoring menu and repainting renderer</name>
  <files>src/PenelopeSMS.App/Menu/MainMenu.cs, src/PenelopeSMS.App/Menu/MonitoringMenuAction.cs, src/PenelopeSMS.App/Rendering/MonitoringScreenRenderer.cs, src/PenelopeSMS.App/Program.cs, tests/PenelopeSMS.Tests/Host/HostBootstrapTests.cs</files>
  <action>Add a first-class monitoring entry in the main menu, register the monitoring menu and rendering components, and implement an auto-refreshing in-place repaint renderer for the shared dashboard and campaign drill-in views.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The app has a dedicated monitoring surface that can repaint cleanly while reading shared monitoring snapshots.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared dashboard behavior and campaign drill-in</name>
  <files>src/PenelopeSMS.App/Menu/MonitoringMenuAction.cs, src/PenelopeSMS.App/Workflows/MonitoringWorkflow.cs, src/PenelopeSMS.App/Rendering/MonitoringScreenRenderer.cs, tests/PenelopeSMS.Tests/Monitoring/MonitoringMenuActionTests.cs</files>
  <action>Build the shared monitoring flow with summary-first campaign rows, explicit toggle for completed campaigns, warnings section, live-delivery panel, and drill-in detail for a selected campaign’s status totals and recent failure or callback samples.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The monitoring UI matches the user’s chosen summary, drill-in, warning, and completed-visibility behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime integration coverage for monitoring behavior</name>
  <files>tests/PenelopeSMS.Tests/Monitoring/MonitoringRuntimeIntegrationTests.cs, tests/PenelopeSMS.Tests/Monitoring/MonitoringMenuActionTests.cs, tests/PenelopeSMS.Tests/Host/HostBootstrapTests.cs</files>
  <action>Add tests that verify shared dashboard composition, campaign drill-in rendering, completed-campaign reveal behavior, and runtime integration of persisted monitoring data plus live operations-monitor snapshots.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>The operator monitor has automated coverage for its shared-screen, drill-in, and runtime-composition behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] The app exposes a shared monitoring menu entry and screen
- [ ] Campaign drill-in shows full status totals and recent failure or callback detail
- [ ] The shared monitor repaints in place while combining persisted summaries, warnings, and live delivery entries
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The console app has a usable shared operator monitoring surface for campaign progress and operational visibility
</success_criteria>

<output>
After completion, create `.planning/phases/05-operator-monitoring/05-03-SUMMARY.md`
</output>
