---
phase: 05-operator-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/PenelopeSMS.Infrastructure/SqlServer/Queries/CampaignMonitoringQuery.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Queries/OperationsIssueQuery.cs
  - src/PenelopeSMS.App/Workflows/IMonitoringWorkflow.cs
  - src/PenelopeSMS.App/Workflows/MonitoringWorkflow.cs
  - src/PenelopeSMS.Infrastructure/DependencyInjection.cs
  - tests/PenelopeSMS.Tests/Monitoring/CampaignMonitoringQueryTests.cs
  - tests/PenelopeSMS.Tests/Monitoring/MonitoringWorkflowTests.cs
autonomous: true
requirements:
  - MON-04
  - OPER-02
must_haves:
  truths:
    - The app can load campaign monitoring rows with all operator-facing recipient states and sort them by recent activity.
    - Completed campaigns stay hidden by default but can be explicitly included by the monitoring workflow.
    - Persisted issue summaries can surface callback issues and recent import or enrichment outcomes without depending on live runtime state.
  artifacts:
    - src/PenelopeSMS.Infrastructure/SqlServer/Queries/CampaignMonitoringQuery.cs
    - src/PenelopeSMS.Infrastructure/SqlServer/Queries/OperationsIssueQuery.cs
    - src/PenelopeSMS.App/Workflows/MonitoringWorkflow.cs
  key_links:
    - Phase 5 requires `queued`, `sent`, `delivered`, and `undelivered` counts in addition to the states already surfaced by send-time summaries.
    - Campaign ordering must reflect recent activity rather than creation order.
    - Callback issues on the main monitor combine unmatched and rejected callback paths even though persistence stays separate underneath.
---

<objective>
Build the SQL-backed monitoring read models and workflow foundation for campaign progress and persisted operational issues.

Purpose: The monitoring screens need a stable data source before runtime instrumentation and repainting UI can be added.
Output: Monitoring queries, workflow read models, DI wiring, and automated coverage for campaign summaries, drill-in data, and callback-issue aggregation.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operator-monitoring/05-CONTEXT.md
@.planning/phases/05-operator-monitoring/05-RESEARCH.md
@.planning/phases/05-operator-monitoring/05-VALIDATION.md
@.planning/phases/04-delivery-pipeline/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add campaign monitoring queries with recent-activity ordering</name>
  <files>src/PenelopeSMS.Infrastructure/SqlServer/Queries/CampaignMonitoringQuery.cs, src/PenelopeSMS.Infrastructure/DependencyInjection.cs, tests/PenelopeSMS.Tests/Monitoring/CampaignMonitoringQueryTests.cs</files>
  <action>Create monitoring-specific SQL queries that return campaign summary rows with counts for `pending`, `submitted`, `queued`, `sent`, `delivered`, `undelivered`, and `failed`, compute a consistent recent-activity timestamp for ordering, and support hiding completed campaigns unless explicitly requested.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The app can load campaign monitor rows in the exact shape and order the operator asked for.</done>
</task>

<task type="auto">
  <name>Task 2: Add persisted issue and drill-in monitoring workflow models</name>
  <files>src/PenelopeSMS.Infrastructure/SqlServer/Queries/OperationsIssueQuery.cs, src/PenelopeSMS.App/Workflows/IMonitoringWorkflow.cs, src/PenelopeSMS.App/Workflows/MonitoringWorkflow.cs, src/PenelopeSMS.Infrastructure/DependencyInjection.cs</files>
  <action>Create workflow-level read models for the shared monitoring screen and campaign drill-in, including callback-issues aggregation from unmatched plus rejected callbacks, summary-only completed job history from persisted tables, and drill-in detail records for selected campaigns.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The monitoring layer has a single query/workflow foundation for campaign summaries, persisted issues, and campaign drill-in data.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated coverage for campaign monitoring rules</name>
  <files>tests/PenelopeSMS.Tests/Monitoring/CampaignMonitoringQueryTests.cs, tests/PenelopeSMS.Tests/Monitoring/MonitoringWorkflowTests.cs</files>
  <action>Add tests proving recent-activity ordering, completed-campaign hiding, full state counts, callback-issues aggregation, and campaign drill-in data shape.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>The SQL-backed monitoring foundation is covered for the operator-facing sorting, filtering, and issue-grouping rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] Campaign monitoring queries expose all required recipient-state counts
- [ ] Completed campaigns are hidden by default and explicitly include-able
- [ ] Callback issues aggregate unmatched and rejected callbacks into one monitoring category
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The codebase has a monitoring read-model foundation for campaign progress and persisted issues
</success_criteria>

<output>
After completion, create `.planning/phases/05-operator-monitoring/05-01-SUMMARY.md`
</output>
