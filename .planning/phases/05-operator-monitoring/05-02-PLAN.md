---
phase: 05-operator-monitoring
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/PenelopeSMS.App/Monitoring/IOperationsMonitor.cs
  - src/PenelopeSMS.App/Monitoring/OperationsMonitor.cs
  - src/PenelopeSMS.App/Monitoring/OperationType.cs
  - src/PenelopeSMS.App/Monitoring/OperationSnapshot.cs
  - src/PenelopeSMS.App/Workflows/ImportWorkflow.cs
  - src/PenelopeSMS.App/Workflows/EnrichmentWorkflow.cs
  - src/PenelopeSMS.App/Workflows/CampaignSendWorkflow.cs
  - src/PenelopeSMS.App/Services/DeliveryCallbackWorker.cs
  - src/PenelopeSMS.App/Program.cs
  - tests/PenelopeSMS.Tests/Monitoring/OperationsMonitorTests.cs
  - tests/PenelopeSMS.Tests/Monitoring/WorkflowMonitoringInstrumentationTests.cs
autonomous: true
requirements:
  - OPER-02
must_haves:
  truths:
    - Import, enrichment, send, and delivery processing publish runtime job lifecycle updates into one shared operations-monitor service.
    - Warnings are tracked newest first and disappear from the active warning view once resolved or the job completes.
    - The delivery worker feeds a persistent live-delivery snapshot without requiring direct console scraping.
  artifacts:
    - src/PenelopeSMS.App/Monitoring/IOperationsMonitor.cs
    - src/PenelopeSMS.App/Monitoring/OperationsMonitor.cs
    - tests/PenelopeSMS.Tests/Monitoring/OperationsMonitorTests.cs
  key_links:
    - The user wants one shared monitoring view, so runtime job state must be centralized rather than spread across menus.
    - Summary-only completed jobs require a separate completed-job snapshot from the verbose active-delivery panel.
    - Repainted monitoring screens need structured snapshots instead of uncontrolled concurrent `Console.WriteLine` output.
---

<objective>
Add the runtime operations-monitor service and instrument the app’s jobs so monitoring screens have live state, warnings, and delivery activity to display.

Purpose: Phase 5 needs a shared runtime source of truth for active jobs and live delivery processing, not just persisted SQL data.
Output: Operations-monitor service, workflow or worker instrumentation, DI wiring, and automated runtime-state coverage.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operator-monitoring/05-CONTEXT.md
@.planning/phases/05-operator-monitoring/05-RESEARCH.md
@.planning/phases/05-operator-monitoring/05-VALIDATION.md
@.planning/phases/05-operator-monitoring/05-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared operations-monitor service</name>
  <files>src/PenelopeSMS.App/Monitoring/IOperationsMonitor.cs, src/PenelopeSMS.App/Monitoring/OperationsMonitor.cs, src/PenelopeSMS.App/Monitoring/OperationType.cs, src/PenelopeSMS.App/Monitoring/OperationSnapshot.cs, src/PenelopeSMS.App/Program.cs</files>
  <action>Create a singleton runtime service that tracks active jobs, recent completed summaries, newest-first active warnings, and a bounded live-delivery buffer for the persistent monitoring panel.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The app has one structured runtime monitoring source for live jobs, warnings, and delivery activity.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument workflows and the delivery worker into runtime monitoring</name>
  <files>src/PenelopeSMS.App/Workflows/ImportWorkflow.cs, src/PenelopeSMS.App/Workflows/EnrichmentWorkflow.cs, src/PenelopeSMS.App/Workflows/CampaignSendWorkflow.cs, src/PenelopeSMS.App/Services/DeliveryCallbackWorker.cs, src/PenelopeSMS.App/Program.cs, tests/PenelopeSMS.Tests/Monitoring/WorkflowMonitoringInstrumentationTests.cs</files>
  <action>Update import, enrichment, campaign send, and delivery processing to publish start, progress, completion, and warning events into the operations monitor while keeping existing operator-visible output behavior coherent.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>Every operator-facing job path contributes consistent lifecycle and warning data to the shared monitoring model.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated coverage for runtime monitoring state transitions</name>
  <files>tests/PenelopeSMS.Tests/Monitoring/OperationsMonitorTests.cs, tests/PenelopeSMS.Tests/Monitoring/WorkflowMonitoringInstrumentationTests.cs</files>
  <action>Add tests proving active-job transitions, newest-first warnings, disappearance of resolved issues from the active warning snapshot, and bounded live-delivery buffering from the delivery worker.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>The runtime monitoring layer is covered for the live/shared visibility rules Phase 5 depends on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] Import, enrichment, send, and delivery jobs publish runtime monitoring snapshots
- [ ] Active warnings are newest first and resolved warnings no longer appear in the main active view
- [ ] The live delivery panel source uses a bounded buffer and remains separate from completed job summaries
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The codebase has a runtime monitoring service that feeds live jobs, warnings, and delivery activity into the shared operator monitor
</success_criteria>

<output>
After completion, create `.planning/phases/05-operator-monitoring/05-02-SUMMARY.md`
</output>
