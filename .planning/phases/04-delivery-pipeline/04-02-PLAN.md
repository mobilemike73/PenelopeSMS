---
phase: 04-delivery-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/PenelopeSMS.Infrastructure/SqlServer/Queries/PendingDeliveryCallbackBatchQuery.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Repositories/DeliveryCallbackRepository.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Repositories/RejectedDeliveryCallbackRepository.cs
  - src/PenelopeSMS.App/Workflows/IDeliveryCallbackProcessingWorkflow.cs
  - src/PenelopeSMS.App/Workflows/DeliveryCallbackProcessingWorkflow.cs
  - src/PenelopeSMS.App/Services/DeliveryCallbackWorker.cs
  - src/PenelopeSMS.Infrastructure/Aws/IAwsSqsClient.cs
  - src/PenelopeSMS.Infrastructure/Aws/AwsSqsClient.cs
  - src/PenelopeSMS.Infrastructure/DependencyInjection.cs
  - tests/PenelopeSMS.Tests/Delivery/DeliveryCallbackRepositoryTests.cs
  - tests/PenelopeSMS.Tests/Delivery/DeliveryCallbackProcessingWorkflowTests.cs
  - tests/PenelopeSMS.Tests/Delivery/DeliveryCallbackWorkerTests.cs
autonomous: true
requirements:
  - MON-02
  - MON-03
must_haves:
  truths:
    - The local app can consume delivery events from SQS and apply them idempotently to the matching `CampaignRecipient`.
    - Duplicate callbacks collapse correctly, older events do not overwrite newer event-time state, and unmatched/rejected events persist through their dedicated paths.
    - The callback processor deletes SQS messages only after successful local persistence.
  artifacts:
    - src/PenelopeSMS.App/Services/DeliveryCallbackWorker.cs
    - src/PenelopeSMS.App/Workflows/DeliveryCallbackProcessingWorkflow.cs
    - src/PenelopeSMS.Infrastructure/SqlServer/Repositories/DeliveryCallbackRepository.cs
  key_links:
    - Current status updates must use the best available provider event time and preserve the reason/source of that timestamp.
    - Exact duplicate callbacks should update `LastSeenAtUtc` on the existing history row rather than create a new row.
    - Failed processing must leave the SQS message available for redelivery or DLQ routing instead of deleting it early.
---

<objective>
Implement the local SQS-to-SQL delivery-processing path that applies callbacks idempotently and keeps current message state plus history in sync.

Purpose: After Phase 4 has a public callback bridge, the local app still needs to consume queue messages continuously, persist delivery history, and protect current status from duplicates and out-of-order callbacks.
Output: SQS client abstraction, delivery-processing repository/workflow, hosted worker, and automated processing tests.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-delivery-pipeline/04-CONTEXT.md
@.planning/phases/04-delivery-pipeline/04-RESEARCH.md
@.planning/phases/04-delivery-pipeline/04-VALIDATION.md
@.planning/phases/04-delivery-pipeline/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQS receive abstractions and delivery-callback persistence logic</name>
  <files>src/PenelopeSMS.Infrastructure/Aws/IAwsSqsClient.cs, src/PenelopeSMS.Infrastructure/Aws/AwsSqsClient.cs, src/PenelopeSMS.Infrastructure/SqlServer/Queries/PendingDeliveryCallbackBatchQuery.cs, src/PenelopeSMS.Infrastructure/SqlServer/Repositories/DeliveryCallbackRepository.cs, src/PenelopeSMS.Infrastructure/SqlServer/Repositories/RejectedDeliveryCallbackRepository.cs, src/PenelopeSMS.Infrastructure/DependencyInjection.cs</files>
  <action>Create the AWS SQS abstraction the app will use for long polling, plus repository support that maps normalized callback envelopes onto matched `CampaignRecipient` rows, collapses duplicates, discards older events, and stores unmatched or rejected callbacks in their dedicated tables.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The app has deterministic persistence rules for matched, duplicate, unmatched, and rejected callback events.</done>
</task>

<task type="auto">
  <name>Task 2: Build the sequential callback-processing workflow and hosted worker</name>
  <files>src/PenelopeSMS.App/Workflows/IDeliveryCallbackProcessingWorkflow.cs, src/PenelopeSMS.App/Workflows/DeliveryCallbackProcessingWorkflow.cs, src/PenelopeSMS.App/Services/DeliveryCallbackWorker.cs, src/PenelopeSMS.App/Program.cs, src/PenelopeSMS.Infrastructure/DependencyInjection.cs</files>
  <action>Implement the sequential long-polling workflow that continuously receives one SQS message at a time, applies the delivery event, emits verbose console output, and deletes the queue message only after successful persistence while leaving failed processing to normal redelivery and DLQ behavior.</action>
  <verify><automated>dotnet build PenelopeSMS.sln && dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The local app runs a continuous sequential callback worker that safely applies queue messages into the delivery ledger.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated tests for idempotent callback application and worker behavior</name>
  <files>tests/PenelopeSMS.Tests/Delivery/DeliveryCallbackRepositoryTests.cs, tests/PenelopeSMS.Tests/Delivery/DeliveryCallbackProcessingWorkflowTests.cs, tests/PenelopeSMS.Tests/Delivery/DeliveryCallbackWorkerTests.cs</files>
  <action>Add tests that verify duplicate callbacks update `LastSeenAtUtc`, older callbacks do not overwrite newer state, unmatched and rejected callbacks persist in the correct store, and SQS messages are deleted only after successful processing.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>The delivery processor has automated coverage for idempotency, out-of-order protection, and queue-deletion safety.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build PenelopeSMS.sln` passes
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] The local worker can process a normalized callback envelope end-to-end in automated tests
- [ ] No callback message is deleted from SQS before successful SQL persistence
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The local app can consume callback envelopes from SQS continuously and apply them idempotently to delivery history
</success_criteria>

<output>
After completion, create `.planning/phases/04-delivery-pipeline/04-02-SUMMARY.md`
</output>
