---
phase: 04-delivery-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/PenelopeSMS.Infrastructure/Twilio/TwilioMessageSender.cs
  - src/PenelopeSMS.App/Menu/CampaignMenuAction.cs
  - src/PenelopeSMS.App/Program.cs
  - src/PenelopeSMS.App/appsettings.json
  - src/PenelopeSMS.CallbackBridge/README.md
  - src/PenelopeSMS.CallbackBridge/aws-lambda-tools-defaults.json
  - tests/PenelopeSMS.Tests/Campaigns/TwilioMessageSenderTests.cs
  - tests/PenelopeSMS.Tests/Host/HostBootstrapTests.cs
  - tests/PenelopeSMS.Tests/Delivery/DeliveryRuntimeIntegrationTests.cs
  - .planning/phases/04-delivery-pipeline/04-USER-SETUP.md
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-03
must_haves:
  truths:
    - New outbound messages are configured to send Twilio delivery callbacks into the deployed callback bridge.
    - The local app starts callback processing continuously with verbose runtime output when the host is running.
    - The phase leaves behind clear deployment and operator setup instructions for the bridge URL, AWS queue, and local app configuration.
  artifacts:
    - src/PenelopeSMS.Infrastructure/Twilio/TwilioMessageSender.cs
    - src/PenelopeSMS.CallbackBridge/README.md
    - .planning/phases/04-delivery-pipeline/04-USER-SETUP.md
  key_links:
    - The `StatusCallback` URL must be attached from configuration rather than hardcoded into code or Twilio console settings.
    - Callback-processing startup should not require manual menu navigation because the context requires continuous operation while the app is open.
    - Deployment/setup documentation must cover the bridge endpoint, SQS queue, DLQ expectation, and local configuration values.
---

<objective>
Complete the delivery pipeline by wiring new sends into the callback bridge, starting the worker continuously in the host, and documenting the deployment/setup path.

Purpose: Phase 4 is only complete when new campaign sends participate in the callback flow, the local app processes callbacks continuously, and the required AWS/Twilio setup is explicit enough to run the pipeline outside automated tests.
Output: Callback-aware send integration, host/runtime wiring, setup documentation, and automated integration coverage.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-delivery-pipeline/04-CONTEXT.md
@.planning/phases/04-delivery-pipeline/04-RESEARCH.md
@.planning/phases/04-delivery-pipeline/04-VALIDATION.md
@.planning/phases/04-delivery-pipeline/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire outbound sends and host startup into the delivery pipeline</name>
  <files>src/PenelopeSMS.Infrastructure/Twilio/TwilioMessageSender.cs, src/PenelopeSMS.App/Program.cs, src/PenelopeSMS.App/appsettings.json, tests/PenelopeSMS.Tests/Campaigns/TwilioMessageSenderTests.cs, tests/PenelopeSMS.Tests/Host/HostBootstrapTests.cs</files>
  <action>Update send-time configuration so `TwilioMessageSender` attaches the configured `StatusCallback` URL, register the delivery worker to start automatically with the host, and extend tests to verify callback-aware send configuration plus worker/host registration.</action>
  <verify><automated>dotnet build PenelopeSMS.sln && dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>New outbound messages participate in the callback pipeline automatically and the local host starts processing callbacks continuously.</done>
</task>

<task type="auto">
  <name>Task 2: Add verbose runtime integration coverage for callback processing</name>
  <files>src/PenelopeSMS.App/Menu/CampaignMenuAction.cs, src/PenelopeSMS.App/Services/DeliveryCallbackWorker.cs, tests/PenelopeSMS.Tests/Delivery/DeliveryRuntimeIntegrationTests.cs</files>
  <action>Add integration-style tests and runtime output refinements that verify verbose callback-processing logs, bridge-to-worker compatibility, and campaign/runtime behavior when delivery events are applied while the app is running.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The runtime callback path has automated coverage beyond isolated repository tests and emits the verbose console output the operator asked for.</done>
</task>

<task type="auto">
  <name>Task 3: Document AWS/Twilio deployment and operator setup for the delivery pipeline</name>
  <files>src/PenelopeSMS.CallbackBridge/README.md, src/PenelopeSMS.CallbackBridge/aws-lambda-tools-defaults.json, .planning/phases/04-delivery-pipeline/04-USER-SETUP.md</files>
  <action>Document how to configure and deploy the callback bridge, connect its public URL to the app’s `StatusCallback` setting, provision the SQS queue plus DLQ, and configure the local console app so a manual live verification run can follow the documented path exactly. Include the supported option of reusing an existing API Gateway via a dedicated route or path mapping instead of provisioning a separate gateway instance.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>The phase leaves behind enough setup guidance for a real callback round-trip test without reverse-engineering the infrastructure choices.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build PenelopeSMS.sln` passes
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] The host starts callback processing automatically in automated tests
- [ ] `04-USER-SETUP.md` documents bridge deployment, queue configuration, and callback URL wiring
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- New messages feed the callback pipeline automatically and the operator has a documented path to run the delivery system end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/04-delivery-pipeline/04-03-SUMMARY.md`
</output>
