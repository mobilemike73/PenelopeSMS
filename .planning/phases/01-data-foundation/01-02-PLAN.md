---
phase: 01-data-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/PenelopeSMS.Domain/Entities/ImportBatch.cs
  - src/PenelopeSMS.Domain/Entities/PhoneNumberRecord.cs
  - src/PenelopeSMS.Domain/Entities/CustomerPhoneLink.cs
  - src/PenelopeSMS.Domain/Services/IPhoneNumberNormalizer.cs
  - src/PenelopeSMS.Domain/Services/PhoneNumberNormalizer.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/PenelopeSmsDbContext.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Configurations/ImportBatchConfiguration.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Configurations/PhoneNumberRecordConfiguration.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Configurations/CustomerPhoneLinkConfiguration.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Repositories/ImportPersistenceService.cs
  - src/PenelopeSMS.Infrastructure/SqlServer/Migrations/*InitialImportSchema.cs
  - tests/PenelopeSMS.Tests/Data/PhoneNumberNormalizerTests.cs
  - tests/PenelopeSMS.Tests/Data/ImportPersistenceTests.cs
autonomous: true
requirements:
  - IMPT-02
  - IMPT-03
  - IMPT-04
must_haves:
  truths:
    - Imported phone data can be normalized into a canonical representation before persistence.
    - Duplicate phone numbers can map to multiple `CUST_SID` associations without duplicating the canonical phone row.
    - Import batches persist counts and timing fields needed for auditability.
  artifacts:
    - src/PenelopeSMS.Infrastructure/SqlServer/PenelopeSmsDbContext.cs
    - src/PenelopeSMS.Domain/Services/PhoneNumberNormalizer.cs
    - src/PenelopeSMS.Infrastructure/SqlServer/Repositories/ImportPersistenceService.cs
  key_links:
    - The normalizer feeds canonical phone values into the persistence service.
    - The DbContext maps canonical phone rows, source associations, and import batches together.
    - Tests exercise normalization and duplicate-association behavior against the persistence model.
---

<objective>
Implement the SQL Server data model, normalization rules, and import-audit persistence needed before Oracle import can be wired.

Purpose: Phase 1 succeeds only if canonical phone data, duplicate handling, and batch audit fields are correct before any source import runs.
Output: Domain entities, normalization service, DbContext and mappings, initial migration, persistence service, and automated tests.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the canonical import domain model and normalization service</name>
  <files>src/PenelopeSMS.Domain/Entities/ImportBatch.cs, src/PenelopeSMS.Domain/Entities/PhoneNumberRecord.cs, src/PenelopeSMS.Domain/Entities/CustomerPhoneLink.cs, src/PenelopeSMS.Domain/Services/IPhoneNumberNormalizer.cs, src/PenelopeSMS.Domain/Services/PhoneNumberNormalizer.cs</files>
  <action>Create the entities and value rules needed for import batches, canonical phone rows, and `CUST_SID` associations. Implement a `libphonenumber-csharp`-backed normalizer that produces a canonical stored format and preserves the original raw input for diagnostics. Do not collapse `CUST_SID` onto the canonical phone entity.</action>
  <verify><automated>dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The domain model cleanly separates canonical numbers from source relationships, and normalization behavior is testable in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Build the SQL Server persistence layer and initial migration</name>
  <files>src/PenelopeSMS.Infrastructure/SqlServer/PenelopeSmsDbContext.cs, src/PenelopeSMS.Infrastructure/SqlServer/Configurations/ImportBatchConfiguration.cs, src/PenelopeSMS.Infrastructure/SqlServer/Configurations/PhoneNumberRecordConfiguration.cs, src/PenelopeSMS.Infrastructure/SqlServer/Configurations/CustomerPhoneLinkConfiguration.cs, src/PenelopeSMS.Infrastructure/SqlServer/Repositories/ImportPersistenceService.cs, src/PenelopeSMS.Infrastructure/SqlServer/Migrations/*InitialImportSchema.cs</files>
  <action>Implement the EF Core DbContext, entity configurations, and a persistence service that can create import batches, upsert canonical phone records, and create `CUST_SID` associations safely. Generate the initial migration for the Phase 1 schema. Favor unique indexes and explicit relationships that prevent accidental duplicate canonical rows.</action>
  <verify><automated>dotnet build PenelopeSMS.sln && dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The SQL Server persistence model builds cleanly, the initial migration exists, and the schema supports canonical numbers plus duplicate lineage.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated tests for normalization, dedupe, and import-audit persistence</name>
  <files>tests/PenelopeSMS.Tests/Data/PhoneNumberNormalizerTests.cs, tests/PenelopeSMS.Tests/Data/ImportPersistenceTests.cs</files>
  <action>Add xUnit tests that verify canonical normalization, duplicate-number association behavior, and import-batch audit updates. Use SQLite-backed EF tests for persistence behavior so the plan does not depend on a live SQL Server instance during execution.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>The data model has automated coverage for canonicalization, duplicate handling, and batch audit behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build PenelopeSMS.sln` passes
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] Initial migration exists for the SQL Server schema
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The persistence model is ready for Oracle import orchestration
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-02-SUMMARY.md`
</output>
