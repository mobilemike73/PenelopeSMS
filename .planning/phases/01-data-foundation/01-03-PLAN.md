---
phase: 01-data-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/PenelopeSMS.Infrastructure/Oracle/OracleCustomerPhoneRow.cs
  - src/PenelopeSMS.Infrastructure/Oracle/IOraclePhoneImportReader.cs
  - src/PenelopeSMS.Infrastructure/Oracle/OraclePhoneImportReader.cs
  - src/PenelopeSMS.App/Workflows/IImportWorkflow.cs
  - src/PenelopeSMS.App/Workflows/ImportWorkflow.cs
  - src/PenelopeSMS.App/Menu/ImportMenuAction.cs
  - src/PenelopeSMS.App/Menu/MainMenu.cs
  - tests/PenelopeSMS.Tests/Import/ImportWorkflowTests.cs
autonomous: true
requirements:
  - IMPT-01
must_haves:
  truths:
    - The operator can run an Oracle-backed import through the console workflow.
    - Oracle rows are mapped through the canonical persistence pipeline instead of bypassing it.
    - Import execution reports usable progress and writes import results into SQL Server.
  artifacts:
    - src/PenelopeSMS.Infrastructure/Oracle/OraclePhoneImportReader.cs
    - src/PenelopeSMS.App/Workflows/ImportWorkflow.cs
    - src/PenelopeSMS.App/Menu/ImportMenuAction.cs
  key_links:
    - The console menu triggers the import workflow.
    - The import workflow reads Oracle rows through an adapter and persists them via the Phase 1 persistence service.
    - Automated tests prove the workflow maps source rows through the canonical persistence path.
---

<objective>
Wire the Oracle import adapter and console workflow onto the Phase 1 schema and persistence foundation.

Purpose: This is the user-visible Phase 1 outcome: a real import path from Oracle into the local system of record.
Output: Oracle import reader, import workflow, menu integration, and automated workflow tests.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Oracle import adapter and row mapping contract</name>
  <files>src/PenelopeSMS.Infrastructure/Oracle/OracleCustomerPhoneRow.cs, src/PenelopeSMS.Infrastructure/Oracle/IOraclePhoneImportReader.cs, src/PenelopeSMS.Infrastructure/Oracle/OraclePhoneImportReader.cs</files>
  <action>Create a read-only Oracle adapter that returns source rows containing `CUST_SID` and phone number fields in a shape the import workflow can consume. Keep Oracle access isolated behind an interface so the workflow can be tested without a live Oracle instance. Do not let Oracle-specific types leak into the domain layer.</action>
  <verify><automated>dotnet build PenelopeSMS.sln</automated></verify>
  <done>The app has a testable Oracle reader abstraction and a concrete ODP.NET implementation for real imports.</done>
</task>

<task type="auto">
  <name>Task 2: Build the import workflow and connect it to the console menu</name>
  <files>src/PenelopeSMS.App/Workflows/IImportWorkflow.cs, src/PenelopeSMS.App/Workflows/ImportWorkflow.cs, src/PenelopeSMS.App/Menu/ImportMenuAction.cs, src/PenelopeSMS.App/Menu/MainMenu.cs</files>
  <action>Implement an import workflow that creates an import batch, streams Oracle rows through the normalization and persistence services, tracks counts, and reports console-visible progress and completion summary. Wire a concrete menu action that invokes the workflow without implementing future verification or campaign steps.</action>
  <verify><automated>dotnet build PenelopeSMS.sln && dotnet test PenelopeSMS.sln --no-restore</automated></verify>
  <done>The menu can trigger a real import workflow, and progress / result counts are surfaced through the console.</done>
</task>

<task type="auto">
  <name>Task 3: Add workflow tests for import orchestration and duplicate handling</name>
  <files>tests/PenelopeSMS.Tests/Import/ImportWorkflowTests.cs</files>
  <action>Add xUnit tests that drive the import workflow with a fake Oracle reader and verify canonical persistence, duplicate association handling, and import-batch counts. Cover at least one case where two source rows normalize to the same canonical phone number but different `CUST_SID` values.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>The import workflow has automated coverage for row mapping, duplicate association behavior, and batch result accounting.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build PenelopeSMS.sln` passes
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] Import workflow reports read/imported/rejected counts through the console path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The operator can launch Oracle import from the console and persist canonicalized results locally
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-03-SUMMARY.md`
</output>
