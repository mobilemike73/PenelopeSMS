---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PenelopeSMS.sln
  - Directory.Build.props
  - src/PenelopeSMS.App/PenelopeSMS.App.csproj
  - src/PenelopeSMS.App/Program.cs
  - src/PenelopeSMS.App/appsettings.json
  - src/PenelopeSMS.App/Menu/MainMenu.cs
  - src/PenelopeSMS.App/Options/OracleOptions.cs
  - src/PenelopeSMS.App/Options/SqlServerOptions.cs
  - src/PenelopeSMS.App/Options/TwilioOptions.cs
  - src/PenelopeSMS.App/Options/AwsOptions.cs
  - src/PenelopeSMS.Domain/PenelopeSMS.Domain.csproj
  - src/PenelopeSMS.Infrastructure/PenelopeSMS.Infrastructure.csproj
  - src/PenelopeSMS.Infrastructure/DependencyInjection.cs
  - tests/PenelopeSMS.Tests/PenelopeSMS.Tests.csproj
  - tests/PenelopeSMS.Tests/Host/HostBootstrapTests.cs
autonomous: true
requirements:
  - OPER-01
user_setup:
  - Oracle connection string / credentials for the real source environment
  - SQL Server Express connection details for the local machine
  - Twilio and AWS credentials to be populated in configuration before later phases
must_haves:
  truths:
    - The solution boots as a .NET 9 host-driven console app and reads configuration from external settings.
    - The operator can provide Oracle, SQL Server, Twilio, and AWS settings without editing source code.
    - The repository has executable test infrastructure for later phase work.
  artifacts:
    - PenelopeSMS.sln
    - src/PenelopeSMS.App/Program.cs
    - src/PenelopeSMS.App/appsettings.json
    - tests/PenelopeSMS.Tests/PenelopeSMS.Tests.csproj
  key_links:
    - Program.cs binds option classes from configuration and builds the main menu service.
    - The test project references the application projects and can run through `dotnet test`.
---

<objective>
Bootstrap the solution, host, configuration model, and test infrastructure for the rest of Phase 1.

Purpose: All later data and import work depends on a stable project structure, externalized configuration, and working automated tests.
Output: Solution and project files, host bootstrap, strongly typed options, a menu shell, and the initial xUnit test project.
</objective>

<execution_context>
@/home/mcoleman/.codex/get-shit-done/workflows/execute-plan.md
@/home/mcoleman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the solution and baseline projects</name>
  <files>PenelopeSMS.sln, Directory.Build.props, src/PenelopeSMS.App/PenelopeSMS.App.csproj, src/PenelopeSMS.Domain/PenelopeSMS.Domain.csproj, src/PenelopeSMS.Infrastructure/PenelopeSMS.Infrastructure.csproj, tests/PenelopeSMS.Tests/PenelopeSMS.Tests.csproj</files>
  <action>Create a `.NET 9` solution with three production projects (`App`, `Domain`, `Infrastructure`) and one xUnit test project. Add project references so the console app depends on `Domain` and `Infrastructure`, and the tests can reference all three. Keep the dependency direction clean: `Domain` should not depend on `Infrastructure`.</action>
  <verify><automated>dotnet build PenelopeSMS.sln</automated></verify>
  <done>The solution restores and builds, and the test project is part of the solution with correct references.</done>
</task>

<task type="auto">
  <name>Task 2: Implement host bootstrap and external configuration binding</name>
  <files>src/PenelopeSMS.App/Program.cs, src/PenelopeSMS.App/appsettings.json, src/PenelopeSMS.App/Menu/MainMenu.cs, src/PenelopeSMS.App/Options/OracleOptions.cs, src/PenelopeSMS.App/Options/SqlServerOptions.cs, src/PenelopeSMS.App/Options/TwilioOptions.cs, src/PenelopeSMS.App/Options/AwsOptions.cs, src/PenelopeSMS.Infrastructure/DependencyInjection.cs</files>
  <action>Use `Host.CreateApplicationBuilder` to build the console host, register the menu shell, and bind strongly typed options for Oracle, SQL Server, Twilio, and AWS from configuration. Provide a minimal main menu shell that can be extended in later plans, but do not implement import logic yet. Keep secrets out of source by using configuration placeholders only.</action>
  <verify><automated>dotnet build PenelopeSMS.sln</automated></verify>
  <done>The app starts through the Generic Host, option classes bind from configuration, and the menu shell is ready for later workflow wiring.</done>
</task>

<task type="auto">
  <name>Task 3: Add smoke tests for host bootstrap and configuration contracts</name>
  <files>tests/PenelopeSMS.Tests/Host/HostBootstrapTests.cs</files>
  <action>Add fast xUnit tests that prove the host can build, key services resolve, and the options classes bind expected configuration keys. Keep the tests environment-agnostic by using in-memory configuration rather than real Oracle or SQL Server connections.</action>
  <verify><automated>dotnet test PenelopeSMS.sln</automated></verify>
  <done>`dotnet test PenelopeSMS.sln` passes with host/bootstrap coverage in place for future plans.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build PenelopeSMS.sln` passes
- [ ] `dotnet test PenelopeSMS.sln` passes
- [ ] App host builds without requiring real external credentials
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The solution structure, host, and config contracts are ready for database and import work
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
